# Summary

<!-- What does this change and why? Link related issues, e.g. "Fixes #12". -->

# Changes

<!-- Bullet the notable changes. Keep the pull request focused on one concern. -->

-

# Checklist

- [ ] Tests added or updated for any behavior change
- [ ] `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` pass locally
- [ ] `npm run generate` produces no diff in `src/generated` (spec and types in sync)
- [ ] README samples and `test/readme-samples.ts` updated together, if samples changed
- [ ] `CHANGELOG.md` updated under `Unreleased`, if the public API changed
